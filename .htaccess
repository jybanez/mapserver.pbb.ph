RewriteEngine On

# Allow direct access to existing files and directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route tile, health, and api endpoints to index.php
RewriteRule ^(tiles/.*|health|api/.*)$ index.php [L,QSA]
